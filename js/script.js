document.getElementById("area").addEventListener("submit", function(event) {
    
        //Show the alert with
        window.alert("Order sent successfully!");

        //Clear the form after send the feedback
        //document.getElementById("feedbackForm").reset();
    });
